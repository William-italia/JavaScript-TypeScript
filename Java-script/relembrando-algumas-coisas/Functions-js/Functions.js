// function soma(x = 1, y = 1) {
//     const resultado = x + y;
//     return `A soma dos valores de ${x} + ${y} é igual a ${resultado}`;
// }

// const resultado = soma(4, 2);
// console.log(resultado);

// const raiz = function (n) {
//     return Math.sqrt(n);
// };

// console.log(raiz(9));

// const pow = (x, y) => {
//   return  Math.pow(x, y);
// };

// console.log(pow(5, 2));

// Função reduzida
const raiz = (n) => { return Math.sqrt(n) };

console.log(raiz(9));