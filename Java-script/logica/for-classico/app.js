// for (let i = 0; i <= 19; i++) {
//     const par = i % 2 === 0 ? 'par' : 'impar';
// }

const frutas = ['maça', 'pêra', 'uva', 'Morango', 'Abacaxi', 'Pessego'];

for (let i = 0; i < frutas.length; i++) {
    console.log(i, frutas[i]);
}