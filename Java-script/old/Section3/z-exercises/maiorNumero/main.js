function Max(x, y) {
    return console.log(x < y ? `${y} é maior que ${x}` : `${x} é maior que ${y}`);
};

const MaiorNumero = Max(55, 200);

function max(x, y) {
    if (x > y) {
        return x
    } else{
        return y
    }
};
const MaXNumber = console.log(max(123, 567));

const max2 = (x, y) => x > y ? x : y; 
 const MaXNum = console.log(max2(3456, 2341));

