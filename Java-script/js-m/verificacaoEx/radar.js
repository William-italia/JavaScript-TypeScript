let speed = 75;

if(speed > 80) {
    console.log(`Velocidade: ${speed}km, está ${speed - 80}km, acima da velocidade permitida, multa`)
} else {
    console.log(`Velocidade: ${speed}, dentro da velocidade permitida da via`)
}